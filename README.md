This repository demonstrates a common, yet easily overlooked, bug in Java: uninitialized instance variables.  The Bug.java file contains code that may lead to unexpected results if the constructor isn't explicitly invoked. The BugSolution.java file offers a corrected version.